# Maintenance Notes

- Validate source values before conversion.
- Keep unit groups isolated so incompatible units are not mixed.
- Check zero and negative values where meaningful.
- Present converted values with consistent precision.